requests>=2.31.0
beautifulsoup4>=4.12.0
supabase>=2.0.0
python-dotenv>=1.0.0
# Playwright is only used by auth_bootstrap.py (one-time human QR login).
# The daily cron uses curl_cffi (no browser).
playwright>=1.45.0
curl_cffi>=0.15.0
